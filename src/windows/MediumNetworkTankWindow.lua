local NetworkTankWindow = require "src.windows.NetworkTankWindow"
local Helpers = require "src.Helpers"

local M = Helpers.shallow_copy(NetworkTankWindow)

M.entity_name = "medium-network-tank"

return M
